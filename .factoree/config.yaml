version: 1
bootstrap:
  command: 'curl -fsSL https://opencode.ai/install | bash'
agents:
  implementer: opencode
  validator: opencode
  reviewer: opencode
sandbox:
  allowedDomains:
    - opencode.ai
    - api.github.com
    - github.com
    - objects.githubusercontent.com
verify:
  commands:
    - name: noop
      run: 'true'
